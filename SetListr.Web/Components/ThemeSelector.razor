 @implements IDialogContentComponent

 <FluentDesignTheme @bind-Mode="@Mode"
                   @bind-OfficeColor="@Color"
                   StorageName="theme" />

 <FluentSelect Label="Theme"
               Items="@(Enum.GetValues<DesignThemeModes>())"
               @bind-SelectedOption="@Mode" />

 <FluentSelect Label="Color"
               Items="@(new OfficeColor?[] { OfficeColor.Access, OfficeColor.PowerApps, OfficeColor.OneNote })"
               @bind-SelectedOption="@Color" />

 @code
 {
     private DesignThemeModes Mode = DesignThemeModes.System;
     private OfficeColor? Color = OfficeColor.Access;
 }